Superfície de conteúdo com raio 20px.

    <Card tone="hero"><SectionLabel onDark>Valor da coleção</SectionLabel>…</Card>

`surface` branca é o padrão; `tinted` para blocos de formulário; `hero` roxa para um único destaque por tela.
