Chip-pílula de filtro; sempre em linha rolável horizontal, com o filtro "Todas" primeiro.

    <Chip selected={filter === "Holo"} onClick={() => setFilter("Holo")}>Holo</Chip>

O selecionado usa tinta roxa e texto `--ds-purple-900` — nunca roxo cheio, que é reservado para ações.
